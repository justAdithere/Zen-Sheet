"use client"
import { useEffect, useState } from 'react'

type Person={id:string;name:string;designation:string}
type BU={id:string;name:string;head:Person;design:Person;content:Person;clusters:Person[];accounts:Person[];designers:Person[];contentTeam:Person[]}
const makePerson=(id:string,label:string):Person=>({id,name:label,designation:''})
const seed={founders:makePerson('founders','Founders'),finance:[makePerson('finance','Finance')],mahima:makePerson('mahima','Mahima'),hr:[makePerson('hr','HR')],operations:[makePerson('operations','Operations')],advisory:[makePerson('advisory','Advisory')],bus:[1,2,3,4].map(i=>({id:`bu${i}`,name:`Business Unit ${i}`,head:makePerson(`bu${i}-head`,'BU Head'),design:makePerson(`bu${i}-design`,'CD – Design'),content:makePerson(`bu${i}-content`,'CD – Content'),clusters:[],accounts:[],designers:[],contentTeam:[]})) as BU[]}
function Card({person,onEdit}:{person:Person;onEdit:(p:Person)=>void}){return <button className="card" onClick={()=>onEdit(person)}><strong>{person.name||'Name'}</strong>{person.designation&&<span>{person.designation}</span>}</button>}
export default function Home(){
 const [data,setData]=useState(seed);const [selected,setSelected]=useState<Person|null>(null)
 useEffect(()=>{const raw=localStorage.getItem('zen-org');if(raw)try{setData(JSON.parse(raw))}catch{}},[]);useEffect(()=>{localStorage.setItem('zen-org',JSON.stringify(data))},[data])
 const edit=(p:Person)=>setSelected({...p});const replace=(p:Person)=>selected&&p.id===selected.id?selected:p
 const save=()=>{if(!selected)return;setData(d=>({...d,founders:replace(d.founders),finance:d.finance.map(replace),mahima:replace(d.mahima),hr:d.hr.map(replace),operations:d.operations.map(replace),advisory:d.advisory.map(replace),bus:d.bus.map(b=>({...b,head:replace(b.head),design:replace(b.design),content:replace(b.content),clusters:b.clusters.map(replace),accounts:b.accounts.map(replace),designers:b.designers.map(replace),contentTeam:b.contentTeam.map(replace)}))}));setSelected(null)}
 const add=(buId:string,type:keyof Pick<BU,'clusters'|'accounts'|'designers'|'contentTeam'>)=>setData(d=>({...d,bus:d.bus.map(b=>b.id===buId?{...b,[type]:[...b[type],makePerson(`${buId}-${type}-${Date.now()}`,type==='clusters'?'Cluster Lead':type==='accounts'?'Account Manager':type==='designers'?'Designer':'Content Lead')]}:b)}))
 return <main><header><div><h1>Zen-Sheet</h1><p>Organization Builder</p></div><button className="reset" onClick={()=>{localStorage.removeItem('zen-org');location.reload()}}>Reset</button></header><section className="canvas"><Card person={data.founders} onEdit={edit}/><div className="toprow"><div className="branch"><h3>Finance</h3>{data.finance.map(p=><Card key={p.id} person={p} onEdit={edit}/>)}</div><div className="branch mahima"><Card person={data.mahima} onEdit={edit}/><div className="support">{[...data.hr,...data.operations,...data.advisory].map(p=><Card key={p.id} person={p} onEdit={edit}/>)}</div><div className="busgrid">{data.bus.map(b=><article className="bu" key={b.id}><h2>{b.name}</h2><div className="leaders"><Card person={b.head} onEdit={edit}/><Card person={b.design} onEdit={edit}/><Card person={b.content} onEdit={edit}/></div><div className="teams"><div><h4>BU Head</h4>{b.clusters.map(p=><Card key={p.id} person={p} onEdit={edit}/>)}{b.accounts.map(p=><Card key={p.id} person={p} onEdit={edit}/>)}<button onClick={()=>add(b.id,'clusters')}>+ Cluster Lead</button><button onClick={()=>add(b.id,'accounts')}>+ Account Manager</button></div><div><h4>Design</h4>{b.designers.map(p=><Card key={p.id} person={p} onEdit={edit}/>)}<button onClick={()=>add(b.id,'designers')}>+ Designer</button></div><div><h4>Content</h4>{b.contentTeam.map(p=><Card key={p.id} person={p} onEdit={edit}/>)}<button onClick={()=>add(b.id,'contentTeam')}>+ Content Lead</button></div></div></article>)}</div></div></div></section>{selected&&<div className="modal"><div className="dialog"><h2>Edit person</h2><label>Name<input value={selected.name} onChange={e=>setSelected({...selected,name:e.target.value})}/></label><label>Designation<input value={selected.designation} onChange={e=>setSelected({...selected,designation:e.target.value})}/></label><div className="actions"><button onClick={()=>setSelected(null)}>Cancel</button><button className="save" onClick={save}>Save</button></div></div></div>}</main>}
